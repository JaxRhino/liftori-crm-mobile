# Moved → docs/skill/liftori-crm-mobile/

The mobile-app build skill is now a full playbook with reference files. Canonical
location:

```
docs/skill/liftori-crm-mobile/
  SKILL.md                      ← main skill (paste this as the skill body)
  reference/security.md         ← security gate (priority)
  reference/scope-template.md    ← dev-team scope intake contract
  reference/design.md            ← design system + field UX
  reference/qa-release.md        ← QA gate, static verify, EAS/OTA
  reference/gotchas.md           ← running lessons log (fold BOLO Go in here)
```

Install via Settings → Capabilities (add the folder, or paste `SKILL.md` and keep the
reference files alongside in the skill directory).
